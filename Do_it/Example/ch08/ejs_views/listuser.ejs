<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>사용자 리스트 페이지</title>
    </head>
    <body>
        <h2>사용자 리스트</h2>
        <div>
            <ul>
                <% for (var i = 0; i < results.length; ++i)
                {
                    var curId = results[i]._doc.id;
                    var curName = results[i]._doc.name; %>
                    <li>#<%= i %> 아이디 : <%= curId %>, 이름 : <%= curName %></li>
                <% } %>
            </ul>
        </div>
        <br><br><a href="/public/login.html">로그인하기</a>
        <br><a href="/public/adduser.html">가입하기</a>
    </body>
</html>