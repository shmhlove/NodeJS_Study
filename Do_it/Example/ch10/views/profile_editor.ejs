<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, height=device-height, initial-scale=1">
		
		<title>프로필 수정 화면</title>
		
		<link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css"> <!-- load bootstrap css -->
    	<link rel="stylesheet" href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.min.css"> <!-- load fontawesome -->
    	
    	<style>
        	body        { padding-top:80px; }
    	</style>
    
	</head>
	<body>
		<form method="post" action="/profile_save">
            <div class="container">			

                <div class="page-header text-center">
                    <h1><span class="fa fa-book"></span>사용자 프로필 수정</h1>
                    <input type="submit" value="저장" name="">
                </div>
                <br>
                <div class="row">
                    <div class="col-sm-6">
                        <div class="well">
                            <% if(user.provider == undefined || user.provider == "") { %>
                                <h3><span class="fa fa-user"></span> 로컬 프로필 정보</h3>
                                <br>
                                <p>
                                    <strong>이메일</strong>: <td><input type="text" name="email" value=<%= user.email %>></td>
                                    <br><br>
                                    <strong>별명</strong>: <td><input type="text" name="name" value=<%= user.name %>></td>
                                </p>
                                <br>
                            <% } else if (user.provider == "facebook") { %>
                                <h3><span class="fa fa-facebook"></span> 페이스북 프로필</h3>
                                <br>
                                <p>
                                    <strong>아이디</strong>: <td><input type="text" name="id" value=<%= user.facebook.id %>></td>
                                    <br><br>
                                    <strong>이메일</strong>: <td><input type="text" name="email" value=<%= user.facebook.email %>></td>
                                    <br><br>
                                    <strong>별명</strong>: <td><input type="text" name="name" value=<%= user.facebook.name %>>
                                </p>
                                <br>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>
        </form>
	</body>
</html>